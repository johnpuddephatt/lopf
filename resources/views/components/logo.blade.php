<div class="overflow-hidden">
    <div
        class="relative flex items-center pl-20 pr-16 mr-2 text-4xl font-bold rounded-full w-80 h-80 -left-12 -top-12 text-{{ $text ?? 'sky'}} bg-{{ $background ?? 'blue' }}">
        <div>
            {{ $siteName }}
        </div>
    </div>
</div>